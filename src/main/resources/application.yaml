spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/order-manager
    username: username
    password: password
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
  mail:
    host: localhost
    port: 1025
    username: ''
    password: ''